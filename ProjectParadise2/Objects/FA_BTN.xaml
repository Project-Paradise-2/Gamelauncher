<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:local="clr-namespace:ProjectParadise2.Objects"
                        xmlns:fa="http://schemas.fontawesome.io/icons/">
    <Style TargetType="{x:Type local:FA_BTN}" x:Key="FA_BTN">
        <Style.Setters>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="local:FA_BTN">
                        <Border 
                                Background="#20232B" 
                                BorderBrush="#393E46" 
                                BorderThickness="0.9" 
                                CornerRadius="14" 
                                Height="{TemplateBinding Height}" 
                                Width="{TemplateBinding Width}">
                            <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                <TextBlock 
                                        Text="{TemplateBinding Property=Content}" 
                                        VerticalAlignment="Center" 
                                        FontSize="14" 
                                        Background="Transparent"  
                                        FontFamily="/Fonts/#Protofo"
                                        Foreground="{TemplateBinding Foreground}" 
                                        Margin="{Binding TextMargin, RelativeSource={RelativeSource TemplatedParent}}">
                                    <TextBlock.Effect>
                                        <DropShadowEffect 
                                                Color="Black" 
                                                BlurRadius="3" 
                                                ShadowDepth="2" 
                                                Opacity="1"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                                <fa:FontAwesome 
                                        Icon="{Binding Icon, RelativeSource={RelativeSource TemplatedParent}}" 
                                        VerticalAlignment="Center" 
                                        HorizontalAlignment="{Binding IconDirection, RelativeSource={RelativeSource TemplatedParent}}"
                                        Margin="{Binding IconMargin, RelativeSource={RelativeSource TemplatedParent}}"
                                        FontSize="20" 
                                        Foreground="{Binding IconColor, RelativeSource={RelativeSource TemplatedParent}}"/>
                            </Grid>
                            <Border.Effect>
                                <DropShadowEffect 
                                        Color="#cc272538" 
                                        BlurRadius="4" 
                                        ShadowDepth="2" 
                                        Opacity="0.5"/>
                            </Border.Effect>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style.Setters>
        <Style.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="Transparent"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>

