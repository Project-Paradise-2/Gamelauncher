<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="{x:Type ComboBox}" x:Key="DropDown">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <Grid>
                        <Border x:Name="Border"
                                Background="#222831"
                                BorderBrush="#393E46"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="5"
                                Height="40"
                                Width="250">
                            <Grid>
                                <!-- Das angezeigte Element (SelectionBoxItem) -->
                                <ContentPresenter x:Name="ContentSite"
                                                  Margin="10,0,30,0"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"/>

                                <!-- Der Pfeil (ToggleButton) -->
                                <ToggleButton x:Name="ToggleButton"
                                              HorizontalAlignment="Right"
                                              VerticalAlignment="Center"
                                              Width="25"
                                              Height="20"
                                              Focusable="False"
                                              Background="Transparent"
                                              BorderBrush="Transparent"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                    <Path Fill="#EEEEEE" 
                                          Data="M 0 0 L 4 4 L 8 0 Z"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                                </ToggleButton>
                            </Grid>
                        </Border>

                        <Popup x:Name="PART_Popup"
                               Placement="Bottom"
                               PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                               StaysOpen="False"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               PopupAnimation="Slide"
                               HorizontalOffset="25"
                               VerticalOffset="0">
                            <Border Background="#222831"
                                    BorderBrush="#393E46"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Width="250">
                                <!-- Entfernen Sie alle unerwünschten Abstände durch das Setzen von Margin auf 0 -->
                                <ScrollViewer Margin="5" CanContentScroll="True">
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- Trigger: Wenn ComboBox nicht fokussiert ist -->
                        <Trigger Property="IsKeyboardFocusWithin" Value="False">
                        </Trigger>
                        <!-- Trigger: Wenn geöffnet -->
                        <Trigger Property="IsDropDownOpen" Value="True">
                        </Trigger>
                        <!-- Trigger: Wenn deaktiviert -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="Transparent"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <!-- Schriftfarbe für die ComboBox -->
        <Setter Property="Foreground" Value="#A9A9A9"/>
    </Style>

    <Style TargetType="{x:Type ComboBoxItem}" x:Key="DropDownItem">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Border x:Name="ItemBorder"
                            Background="#222831"
                            Padding="5">
                        <ContentPresenter VerticalAlignment="Center"
                                          HorizontalAlignment="Left"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Trigger: Wenn ausgewählt -->
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Foreground" Value="#222831"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <!-- Keine Hover-Effekte und keine zusätzlichen Triggers -->
        <Setter Property="Foreground" Value="Transparent"/>
    </Style>
</ResourceDictionary>
